package types

// Core types that can be shared across packages without circular dependencies

// This file can contain basic types that are used by multiple packages
// For now it's empty, but we'll use it to break circular dependencies 